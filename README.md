# Tushar Gala Real Estate

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set the `GROQ_API_KEY` in [.env.local](.env.local) to your Groq API key (free at console.groq.com)
3. Run the app:
   `npm run dev`
